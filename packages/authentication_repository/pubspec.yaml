name: authentication_repository
description: Dart package which manages the authentication.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  appwrite_repository:
    path: ../appwrite_repository
  employees_repository:
    path: ../employees_repository
  rxdart: ^0.27.7

dev_dependencies:
  very_good_analysis: ^3.1.0

